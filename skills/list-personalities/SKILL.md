---
description: List all personalities currently in the rotation. Use when the user asks "what personalities are there", "list personalities", "show me the personas", or similar.
disable-model-invocation: false
---

# List Personalities

Run the plugin's list script and show its output to the user as-is — it's already formatted as
a markdown list, no need to reformat or summarize it further:

```
~/.claude/skills/personalities/scripts/list-personalities.sh
```
